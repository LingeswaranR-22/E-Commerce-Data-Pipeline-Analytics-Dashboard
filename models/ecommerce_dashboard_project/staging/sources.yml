version: 2

sources:
  - name: public
    database: ecommerce
    schema: public
    tables:
      - name: orders
        columns:
          - name: order_id
            tests: [not_null, unique]
          - name: customer_id
            tests: [not_null]
          - name: product_id
            tests: [not_null]
          - name: order_date
            tests: [not_null]
          - name: total_amount
            tests: [not_null]

models:
  - name: stg_orders
    columns:
      - name: order_id
        tests: [not_null, unique]
      - name: customer_id
        tests: [not_null]
      - name: product_id
        tests: [not_null]
      - name: order_date
        tests: [not_null]
      - name: total_amount
        tests: [not_null]


